{{/* Lock server — ServiceAccount for Hazelcast StatefulSet pods */}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: hazelcast
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "kramerius.labels" . | nindent 4 }}
    app.kubernetes.io/name: hazelcast
    app.kubernetes.io/component: lock-server
