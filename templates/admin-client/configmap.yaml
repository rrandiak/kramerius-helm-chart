{{- if .Values.adminClient.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: admin-client-globals
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "kramerius.labels" . | nindent 4 }}
data:
  globals.js: |
    {{- include "kramerius.adminClient.globalsJs" . | nindent 4 }}
{{- end }}
