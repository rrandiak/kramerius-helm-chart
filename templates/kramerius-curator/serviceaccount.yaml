{{/* Kramerius Curator — ServiceAccount for StatefulSet pods */}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: kramerius-curator
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "kramerius.labels" . | nindent 4 }}
    app.kubernetes.io/name: kramerius-curator
